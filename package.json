{
  "name": "ts-layout",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vitest --globals",
    "test": "vitest run --globals",
    "build": "./build.sh"
  },
  "devDependencies": {
    "esbuild": "^0.25.10",
    "typescript": "~5.8.3",
    "vitest": "^3.2.4"
  }
}
